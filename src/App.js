import React from "react";
import { Header } from "../src/components/layout/Header";
export const App = () => (
  <div className="App">
    <header className="App-header">
      <Header />
    </header>
  </div>
);
